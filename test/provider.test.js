import React from 'react';

import { mount, shallow } from 'enzyme';
import { expect } from 'chai';

import ProviderContainer from './fixtures/ProviderContainer'

describe('Provider Component', () => {
  it.skip('renders children', () => {
    // const wrapper = shallow(<ProviderContainer><div>hi</div></ProviderContainer>);
    // expect(wrapper.children(0)).to.have.length.of(1);
  });
  it.skip('adds configurations to context', () => {
    
  });
});