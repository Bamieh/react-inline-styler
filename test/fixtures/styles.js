const stylesToInject = () => {
  return {
    defaultRootStyle: {
      position: 'absolute',
      start: 0,
      fontSize: '2rem',
      borderRadius: 4,
      border: '1px solid black',
    },
  }
}

export default stylesToInject